# ============================================================
# UET Prospectus RAG Pipeline — Python Dependencies
# Tested on Python 3.13 (Windows)
# ============================================================

# PDF parsing & table extraction
pdfplumber>=0.11.0
pymupdf>=1.24.0           # fitz — page-to-image rendering for OCR fallback

# OCR fallback (requires Tesseract binary; see README)
pytesseract>=0.3.10
Pillow>=10.4.0

# Embeddings (local, no API cost)
sentence-transformers>=3.3.0
torch>=2.9.0              # CPU; use the version pip resolves for Python 3.13

# numpy — pin to 2.x so pip picks a pre-built wheel
numpy>=2.0.0

# Vector store
chromadb>=0.5.23

# LLM via Groq
groq>=0.11.0

# Chat UI
chainlit>=2.11.0

# Async loop fix (needed on some Windows setups for Chainlit's server)
nest-asyncio>=1.6.0

# Utilities
python-dotenv>=1.0.0
tiktoken>=0.8.0
tqdm>=4.67.0